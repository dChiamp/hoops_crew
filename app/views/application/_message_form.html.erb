<%= form_for @message, method: :post do |f| %>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
     <!-- follow for pic etc -->
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-success"%>
  </div>
  
<% end %>
